# GitHub-репозитории по Zynq-платам

Локальные копии лежат в `c:\code\`. Два репозитория EBAZ4205 переименованы, чтобы не конфликтовать по имени папки.

## EBAZ4205 (XC7Z010)

Плата управления майнером Ebit E9+ — дешёвая отладочная платформа на Zynq-7000 с 256 MB DDR3, 128 MB NAND, Ethernet 100M. По умолчанию загрузка с NAND; для SD-карты нужно припаять разъём и переставить резистор boot select.

| Репозиторий | Локальная папка | О чём | Что сделано |
|---|---|---|---|
| [Elrori/EBAZ4205](https://github.com/Elrori/EBAZ4205) | `c:\code\EBAZ4205-Elrori` | Документация и аппаратная база: KiCad плата расширения EBAZEXT, архив SD-boot; ссылки на статьи (bare-metal, u-boot/Linux, HDMI) и OpenWrt-сборку ([Elrori/openwrt-auto](https://github.com/Elrori/openwrt-auto)). | Полный KiCad-проект платы расширения **EBAZEXT-V2** (HDMI, USB-C/UART CH340, CMOS, аудио, GPIO, разъёмы к FPGA); XDC для V1 (VGA, CMOS, MII); архив SD-boot (DTS, uEnv, ps7_init для Vivado 2018.3); pinout Zynq-7010. Bare-metal, сборка u-boot/Linux, HDMI soft-decode и OpenWrt — только внешние ссылки; FPGA-кода, gerber и готовых образов нет. |
| [xjtuecho/EBAZ4205](https://github.com/xjtuecho/EBAZ4205) | `c:\code\EBAZ4205-xjtuecho` | Основной англоязычный репозиторий по плате: [wiki](https://github.com/xjtuecho/EBAZ4205/wiki), [discussions](https://github.com/xjtuecho/EBAZ4205/discussions), схема PDF, файлы Altium/KiCad, фото, `proc/cmdline`, инструкции по питанию, JTAG, UART, сбросу пароля root и отключению cgminer. | KiCad-проект платы (PCB + пересобранная иерархическая схема V004), PDF-схема, Altium PCB, оригинальный Protel PCB, STL-подставка, фото; инструкции по штатному Linux/u-boot (сброс пароля, отключение cgminer, сеть); дампы `/proc` и логи загрузки; шаблон XDC для Vivado (пины закомментированы). Собственной прошивки, bitstream, ядра и u-boot нет. |
| [blkf2016/ebaz4205](https://github.com/blkf2016/ebaz4205) | `c:\code\ebaz4205-blkf2016` | Исходники и конфиги для сборки Linux на SD (Buildroot 2019.05, Linux 5.1.5, U-Boot 2019.07-rc4, Vivado 2019.1). Целевой образ: root/root. | BSP для самостоятельной сборки: модифицированный FSBL (загрузка U-Boot с SD), оверлей Buildroot 2019.05 (DTS, конфиги ядра/U-Boot, boot.scr, genimage), rootfs overlay (статический IP 192.168.100.1, dnsmasq), скрипт Vivado 2019.1 и XDC. Готовых бинарников и SD-образов в репозитории нет — только исходники и конфиги. |

См. также: [EBAZ4205 — ресурсы](EBAZ4205%20-%20ресурсы.md), [EBAZ4205 — прошивка](EBAZ4205-%20прошивка.md).

## Antminer S9 (XC7Z010)

Плата управления Antminer S9 — Zynq-7010 с flash, Fast Ethernet и слотом SD. Больше GPIO, чем у EBAZ4205.

| Репозиторий | Локальная папка | О чём | Что сделано |
|---|---|---|---|
| [KarolNi/S9miner_sample](https://github.com/KarolNi/S9miner_sample) | `c:\code\S9miner_sample` | Учебные bare-metal примеры Vivado/SDK для S9: `P01_HELLO`, `P02_GPIO`, `P03_SD_TEST`, `P04_SD_BOOT`. Схема платы управления в PDF. | 4 учебных проекта Vivado/SDK (bare-metal, без PetaLinux): Hello World по UART; GPIO (кнопки/LED платы AntMiner); тест SD-карты (FatFs, запись/чтение); SD-boot (FSBL + приложение, мигание LED). Только PS7 без PL-логики; готовые bitstream и BOOT.BIN не включены. |
| [polprog/antminer_zynq](https://github.com/polprog/antminer_zynq) | `c:\code\antminer_zynq` | Начальный сборник по control board S9: фото, U-Boot DTS, boot log. Планируются Vivado, XSA, PetaLinux и готовые образы для SD/TFTP. GPL-2.0 (кроме схем Bitmain). | Начальная заготовка: README, фото платы, U-Boot DTS (`bitmain-antminer-s9`), лог загрузки штатной прошивки (FSBL/U-Boot/Linux/Angstrom/bmminer). Vivado, XSA, PetaLinux, boot-образы и kernel DTS не реализованы — только заявлены в плане. |

См. также: [Antminer — ресурсы](Antminer%20-%20ресурсы.md).

---

## Привязка к плате (как и где зафиксирована)

На Zynq «привязка» обычно делается в трёх слоях: **PS7** (MIO/EMIO в Vivado BD или `.tcl`), **XDC** (физические BGA-пины для сигналов PL/EMIO) и **DTS** (описание для Linux/U-Boot). Подробнее о терминах — в [Zynq — термины и чек-лист прошивки](Zynq%20-%20термины%20и%20чек-лист%20прошивки.md).

### Elrori/EBAZ4205 (`EBAZ4205-Elrori`)

Привязка **разрозненная**, полного Vivado-проекта в репозитории нет.

**XDC (активный, PL-пины):** `EBAZEXT-V1/ebazextv1.xdc` — единственный рабочий constraint-файл. Плата расширения EBAZEXT: LED, кнопки, CMOS, VGA, UART, аудио на PL-пинах. Блок «EBAZ4205 pin»: LED `W13`/`W14`, Ethernet **MII** и MDIO (`U14`…`Y19`, `W15`, `Y14`) — те же BGA-пины, что и в blkf2016. Способ: прямые `PACKAGE_PIN` на порты top-модуля, без отдельного PS7-скрипта в репо.

**DTS (Linux, архив):** `archive/2018.3-1/zynq-ebaz4205.dts` — кнопки S2/S3 на **MIO** gpio 20/32; LED на **EMIO** gpio 54/55; `&gem0` с `phy-mode = "mii"`; консоль `&uart1`; SD `&sdhci0`. Предполагает Vivado-конфиг с EMIO GPIO и Ethernet, но соответствующего BD/XDC в репозитории нет.

**Прочее:** `archive/.../ps7_init_gpl.c` — таблицы инициализации DDR/PS (Vivado 2018.3), не карта пинов. KiCad **EBAZEXT-V2** — аппаратная привязка разъёмов расширения на уровне схемы/PCB. `xc7z010clg400pkg.txt` — справочник pinout кристалла.

**Логика (PS / PL):** репозиторий про **документацию и аппаратную базу**, не про готовую прошивку. **PS:** в репо нет Vivado BD и прикладного кода; для Linux задано только описание периферии в `zynq-ebaz4205.dts` (кнопки, LED, Ethernet MII, UART, SD) и загрузочный сценарий `uEnv.txt` (чтение ядра/DTB с SD через U-Boot). `ps7_init_gpl.c` — инициализация DDR/тактов PS, не логика приложения. Статьи по bare-metal, HDMI soft-decode и OpenWrt — вне репозитория. **PL:** RTL (Verilog/VHDL) **отсутствует** — XDC задаёт только имена портов и BGA-пины, но не реализует схему. KiCad **EBAZEXT-V2** описывает задуманную PL-периферию на плате расширения: HDMI OUT, USB-C + CH340G (UART), параллельная CMOS-камера, аудио, GPIO; для работы нужен отдельный FPGA-проект (TMDS-энкодер, захват CMOS и т.д.), которого в репо нет. Блок MII/LED в XDC предполагает top-модуль с Ethernet и светодиодами в fabric, но исходников такого модуля нет.

**Итог:** XDC для PL и MII есть; DTS для Linux есть; связующего Vivado-проекта (PS7 + RTL + согласованный XDC) — нет.

### xjtuecho/EBAZ4205 (`EBAZ4205-xjtuecho`)

Привязка **только как документация**, активной прошивочной конфигурации нет.

**KiCad:** `HW/ebaz4205/kicad/` — схема V004 и PCB; физическая карта платы (разъёмы DATA1–3, питание, DDR, PHY). Это HW-источник истины для пинов, но не файл для Vivado.

**XDC (шаблон, неактивный):** `Development/EBAZ4205.xdc` — все строки закомментированы. Справочник: DATA1/2/3 → BGA-пины, LED `W13`/`W14`, разъёмы J3/J5. Чтобы использовать, нужно раскомментировать и переименовать порты под свой top-модуль.

**Дампы штатной ОС:** `proc/`, `DOC/` — как заводская прошивка видит NAND, UART, разделы; не задают новую привязку.

**Логика (PS / PL):** репозиторий про **эксплуатацию штатной прошивки майнера**, не про новый FPGA-проект. **PS:** на плате из коробки крутится Linux 4.6 (Angstrom) с **cgminer** — управление майнингом целиком на ARM: сеть, мониторинг, обмен с хеш-платами через разъёмы DATA1–3. В репо это задокументировано инструкциями (сброс пароля, отключение `cgminer`, статический IP) и дампами `proc/`, `DOC/Boot.log`. Собственного PS-кода (C/Linux) в репозитории нет. **PL:** штатный bitstream **`top_ctrl`** (Vivado 2016.4, ~2 МБ) грузится из NAND **до** ядра (`fpga loadb` в u-boot); по имени и контексту — логика **управления майнером** (интерфейс к хеш-платам, возможно PWM/сигналы на J3/J5). Исходников PL в репо нет — только следы в boot log (`design filename = "top_ctrl"`). Раздел NAND `nand-bitstream` хранит этот bitstream.

**Итог:** нет Vivado BD/tcl, нет DTS, нет активного XDC. Только KiCad + закомментированный шаблон пинов + документация штатной PS/PL-прошивки майнера.

### blkf2016/ebaz4205 (`ebaz4205-blkf2016`)

Привязка **наиболее полная** из EBAZ-репозиториев: PS7 + XDC + DTS согласованы.

**PS7 (`ebaz4205.tcl`):** Block Design с `processing_system7_0` и минимальной PL-обвязкой. На **MIO** (XDC не нужен): NAND MIO 0, 2–14; UART1 (консоль) MIO 24–25; SDIO0 MIO 40–45; MIO GPIO (кнопки S2/S3 на MIO 20, 32). На **EMIO** (нужен XDC): UART0, 2 линии GPIO (LED), Ethernet GMII + MDIO; тактовые линии ETH — external.

**XDC (`ebaz4205.xdc`):** EMIO → BGA: UART0 `F19`/`F20`; LED `W14` (gpio 54), `W13` (gpio 55); Ethernet `U14`…`Y19`, MDIO `W15`/`Y14`.

**DTS (`buildroot/board/ebaz4205/zynq-ebaz4205.dts`):** `led_r`/`led_g` → gpio 54/55; кнопки → gpio 20/32; `&uart1` консоль, `&uart0` второй UART; `&sdhci0`; `&gem0` с `phy-mode = "rgmii-id"` (на плате PHY обычно **MII** — возможное расхождение с железом).

**Логика (PS / PL):** проект про **свой Linux на SD** вместо штатного майнера. **PS:** (1) **модифицированный FSBL** (`main.c` + `uboot_loader.c`) — при загрузке с SD, если в `BOOT.BIN` нет приложения, читает `u-boot.bin` с FAT и передаёт управление на `0x04000000`; (2) **Buildroot rootfs** — минимальный Linux (OpenSSH, сеть); overlay задаёт статический IP `192.168.100.1` и dnsmasq; (3) драйверы ядра для UART, SD, GPIO, Ethernet — через DTS. Вся прикладная логика на ARM, майнер отсутствует. **PL:** не пользовательское приложение, а **обвязка Ethernet**: IP `xlconcat` + `xlconstant` + `xlslice` в BD подгоняют 4-битный PHY-интерфейс платы к 8-битному GMII порту PS7 (дополнение нулей на RX, срез TX). Это единственная «логика» в fabric; RTL на Verilog нет. Bitstream нужен для вывода EMIO на пины, но по сути — маршрутизация, не ускоритель.

**Итог:** эталонный BSP «голый Linux на SD»; перед сборкой стоит сверить `phy-mode` в DTS с реальным PHY.

### KarolNi/S9miner_sample (`S9miner_sample`)

Привязка **только через PS7 (MIO)** в Vivado Block Design; пользовательского XDC нет.

**Vivado BD (`P0x_*/.../zynq.bd`):** только `processing_system7_0`, PL пустой, **EMIO отключён** (`PCW_EN_EMIO_GPIO = 0`). Ethernet в BD **не включён**. Файлы `zynq_ooc.xdc` — служебные (OOC-синтез), не карта платы.

**MIO (одинаково в P02–P04):** NAND MIO 0, 2–14; UART1 MIO 48–49; SDIO0 MIO 40–45; остальное — MIO GPIO. В **P01_HELLO** дополнительно card detect SD на MIO 34.

**Код приложений (bare-metal):** номера GPIO = номера MIO, заданы в `main.c` вручную:

| Сигнал | MIO (gpio#) | Проекты |
|--------|-------------|---------|
| LED (общий) | 15 | P02 |
| LED_R | 37 | P02, P04 |
| LED_G | 38 | P02, P04 |
| Кнопка S1 | 47 | P02 |
| Кнопка S2 | 51 | P02 |

**DTS:** в репозитории нет. Привязка живёт в BD + константах в C.

**Логика (PS / PL):** набор **учебных bare-metal тестов** под S9, без Linux и без майнинга. **PS** — вся логика на Cortex-A9:

| Проект | Назначение | Где прописано |
|--------|------------|---------------|
| **P01_HELLO** | Проверка UART: вывод «Hello World» | `helloworld.c`; UART1 в BD (MIO 48–49) |
| **P02_GPIO** | Кнопки S1/S2 → LED; опрос `XGpioPs` | `main.c` (#define MIO 47/51/37/38/15); BD: MIO GPIO |
| **P03_SD_TEST** | Запись/чтение `test.txt` на SD через FatFs | `main.c` + `sd.c`; BD: SDIO0 (MIO 40–45) |
| **P04_SD_BOOT** | Цепочка FSBL→приложение с SD; мигание LED_R/G | `main.c` + Xilinx FSBL + `SD_BOOT.bif`; BD как P02 |

Периферия включается в **PS7 MIO** (`.bd`); приложение обращается к ней через Xilinx BSP (`XGpioPs`, `xilffs`). **PL:** пустой — только оболочка `processing_system7_0`, пользовательских IP и RTL нет. Bitstream либо отсутствует, либо минимальный (конфигурация PS без fabric-логики). Интерфейс к хеш-платам и Ethernet не затрагиваются.

**Итог:** учебные PS-примеры на MIO; PL не используется. Для Linux нужен отдельный DTS (см. polprog) и включение Ethernet в PS7.

### polprog/antminer_zynq (`antminer_zynq`)

Привязка **только декларативная на уровне U-Boot DTS**; аппаратного проекта нет.

**DTS (`uboot_bitmain-antminer-s9.dts`):** адаптация Xilinx DTS для S9. Включены: `&gem0` (`phy-mode = "rgmii-id"`, PHY addr 1); `&uart1` (консоль); `&sdhci0`; `&nfc0` / `&smcc` (NAND); RAM 1 ГБ (`0x40000000`); `reserved-memory` для FPGA (`0xf000000`). **Нет** узлов `gpio-leds` / `gpio-keys` — LED и кнопки в DTS не описаны.

**Нет:** Vivado-проекта, XDC, Block Design, kernel DTS, XSA (в репо есть `zynq_wrapper_p01_hello.xpr.xsa` из чужого hello-проекта, не привязанный к S9).

**Логика (PS / PL):** репозиторий — **заготовка под будущий Linux/PetaLinux**, не рабочая прошивка. **PS:** единственный артефакт — U-Boot DTS, декларирующий ожидаемую периферию PS: Ethernet (`&gem0`), UART-консоль, SD, NAND flash, watchdog. `bootlog1.log` показывает **штатную** PS-логику Bitmain: FSBL 2015.4 → U-Boot 2014.01 → Linux 3.14 (Angstrom) → **bmminer** (аналог cgminer на S9). Это поведение заводской прошивки, не этого репо. **PL:** в DTS зарезервировано 16 МБ по адресу `0x0F000000` (`fpga_space`) — типичная область для **AXI-регистров управления хеш-платами** в штатном bitstream. Boot log подтверждает загрузку bitstream из NAND до U-Boot. Собственной PL-логики (RTL, bitstream, описания регистров) в репозитории нет. Планируемые Vivado/XSA/PetaLinux-образы должны либо воспроизвести штатный `top`-bitstream, либо заменить его своей fabric-логикой.

**Итог:** ориентир «какое железо и память ожидать»; PS/PL-код не реализован. Для прошивки нужен свой Vivado + kernel DTS + сверка с PDF-схемой и KarolNi.

### Сводка

| Репозиторий | PS7 / BD | XDC | DTS | Привязка | Логика PS | Логика PL |
|---|---|---|---|---|---|---|
| Elrori | нет | активный (PL+MII) | есть (архив) | XDC + DTS | DTS, uEnv, ps7_init; нет кода | XDC + KiCad EBAZEXT; **нет RTL** |
| xjtuecho | нет | шаблон (выкл.) | нет | KiCad + справочник | Документация штатного Linux/cgminer | Штатный `top_ctrl` в NAND; **нет исходников** |
| blkf2016 | tcl | активный (EMIO) | есть | PS7 MIO + XDC EMIO + DTS | FSBL→U-Boot, Buildroot Linux | Только обвязка EMIO Ethernet (xlconcat/slice) |
| KarolNi | bd (4 проекта) | служебный | нет | MIO в BD + C | 4 bare-metal теста (UART/GPIO/SD/boot) | **Пустой** fabric |
| polprog | нет | нет | U-Boot DTS | Описание для загрузчика | DTS + boot log штатного bmminer | `reserved-memory` для FPGA; **нет bitstream/RTL** |

## Что из репозиториев — части проекта/прошивки

### EBAZ4205

| Репозиторий | Прошивка / проект | Только документация / HW |
|-------------|-------------------|---------------------------|
| **Elrori** | XDC, DTS, uEnv, ps7_init (фрагменты SD-boot) | KiCad EBAZEXT-V2, pinout, ссылки |
| **xjtuecho** | Шаблон XDC (неактивный) | KiCad, PDF, логи, `/proc`, инструкции |
| **blkf2016** | **Почти полный BSP**: FSBL, Buildroot-оверлей, DTS, U-Boot/kernel configs, boot.scr, genimage, Vivado TCL/XDC | Нет готовых `.bin`/`.img` |

### Antminer S9

| Репозиторий | Прошивка / проект | Только документация |
|-------------|-------------------|---------------------|
| **KarolNi** | 4 Vivado/SDK проекта (BD PS7, bare-metal apps, FSBL+BIF в P02/P04) | PDF схемы |
| **polprog** | U-Boot DTS (один файл) | Фото, boot log, README |

---

## Практическая карта: «что взять из какого репо»

**EBAZ4205, Linux на SD (максимально полный путь из имеющегося):**

1. Схема и пины — **xjtuecho** (KiCad, XDC-шаблон) + **Elrori** (рабочий DTS/uEnv)
2. Сборка — **blkf2016** (FSBL, Buildroot, Vivado TCL)
3. Плата расширения HDMI — отдельно **Elrori EBAZEXT-V2** + свой PL + драйверы

**Antminer S9, bare-metal:**

1. Примеры — **KarolNi** P01→P04
2. DTS для ориентира — **polprog**
3. Полный Linux — **собирать самому** по аналогии с blkf2016 + DTS из polprog/Xilinx

